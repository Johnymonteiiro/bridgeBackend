{
    "version": 2,
    "build":[
        {
            "src": "src/server/server.ts",
            "use": "@vercel/node"
        }
    ],

    "routes": [
        {
             "src": "(.*)",
             "dest": "src/server/server.ts"
        }
    ]
}